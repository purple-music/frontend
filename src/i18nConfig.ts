import { I18nConfig } from "@/lib/types";

const i18nConfig: I18nConfig = {
  locales: ["en", "ru"],
  defaultLocale: "ru",
  prefixDefault: false,
  localeCookie: "NEXT_LOCALE",
  basePath: "",
  cookieOptions: undefined,
};

export default i18nConfig;
